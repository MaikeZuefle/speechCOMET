speech_regression_metric:
  class_path: speechcomet.models.SpeechRegression
  init_args:
    nr_frozen_epochs: 0.3
    keep_trg_embeddings_frozen: True
    keep_src_embeddings_frozen: False  # trains Whisper via LoRA; set True to freeze
    lora_r: 8
    lora_alpha: 16
    lora_dropout: 0.0
    optimizer: AdamW
    encoder_learning_rate: 1.0e-06
    learning_rate: 1.5e-05
    layerwise_decay: 0.95
    encoder_model: XLM-RoBERTa
    pretrained_model: xlm-roberta-base  # or microsoft/infoxlm-large
    encoder_model_audio: openai/whisper-small
    input_modality: audio
    pool_audio: attn  # or avg
    pool: avg
    layer: mix
    layer_transformation: sparsemax
    layer_norm: False
    dropout: 0.1
    batch_size: 2
    train_data:
      - maikezu/iwslt2026-metrics-shared-train-dev  # pretrain on maikezu/wmt-human-all-TTS first for best results
    validation_data:
      - maikezu/iwslt2026-metrics-shared-train-dev

trainer: ../../trainer.yaml
early_stopping: ../../early_stopping.yaml
model_checkpoint: ../../model_checkpoint.yaml
